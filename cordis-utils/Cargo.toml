[package]
name = "cordis-utils"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"
description = "Shared utilities for cordis-rs — List, merge_configs, format_date"
license = "MIT"
homepage = "https://github.com/Ricardo-M-L/cordis-rs"
repository = "https://github.com/Ricardo-M-L/cordis-rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
